# Pinned development/test/lint dependencies (a known-good resolved set).
# Install: pip install -r requirements-dev.txt -e ".[dev]"
# Regenerate after upgrading: pip install -e ".[dev,prod]" && pip freeze | findstr /R "pytest mypy django-stubs ruff"
-r requirements.txt
pytest==9.0.3
pytest-django==4.12.0
pytest-cov==7.0.0
mypy==2.0.0
django-stubs==6.0.4
ruff==0.15.1
